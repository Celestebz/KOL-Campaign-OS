PORT=5001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=kol_user
DB_PASSWORD=kol_password
DB_NAME=kol_campaign_os
DB_NAME_TEST=kol_campaign_os_test
DB_ROOT_PASSWORD=root_password
DB_CONNECTION_LIMIT=10
# Personal-login cookie signing secret. Generate a long random value in production.
SESSION_SECRET=replace-with-a-long-random-secret
# Optional override. Leave empty to detect HTTP/HTTPS from each request.
COOKIE_SECURE=
